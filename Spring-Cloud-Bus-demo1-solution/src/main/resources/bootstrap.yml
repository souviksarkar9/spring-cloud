spring:
  application:
    name: demo-solution-service
  config:
    name: configserver
    enabled: true
    uri: http://localhost:8888
  rabbitmq:
    host: localhost  
    port: 5672
    username: guest
    password: guest
  cloud:
    bus:
      enabled: true
      refresh:
        enabled: true
    

     